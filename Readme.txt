The app.js file is the main web server that is being done by express.
App is also using ejs to render templates, redis to store sessions and the
mysql package that is used to communicate with the databse.

